# Bootstrap placeholder file for sops-nix.
#
# This file intentionally contains no real secrets. Replace or re-encrypt it
# with `sops` before defining any `sops.secrets.*` entries that depend on it.
example:
  note: REPLACE_WITH_ENCRYPTED_CONTENT
