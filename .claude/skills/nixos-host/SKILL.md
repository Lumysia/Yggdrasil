---
name: nixos-host
description: Use when adding, removing, or changing NixOS host definitions under nixos/hosts, nixos/flake.nix nixosConfigurations, hostnames, or related Komodo server names.
---

# NixOS Host

The source of truth for this skill is `.opencode/skills/nixos-host/SKILL.md` (relative to the repo root). Read that file now and follow its instructions exactly. Resolve any relative paths mentioned inside it against `.opencode/skills/nixos-host/`, not against this file.
